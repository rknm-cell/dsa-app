import { arraynums, arraynums2, arraynums3 } from "./constants";


export default function insertionSort(array: number[]):(number[]){

    const arr = array

    for (let i = 0; i > arr.length; i ++){
        //iterate through array the length of the array
        for (let j = i; j < 0; j--){
            if (arr[i] > arr[j]){

            } 
            arr.splice(arr[j], 1)
            arr.


        }
    }


return sortingArray
}